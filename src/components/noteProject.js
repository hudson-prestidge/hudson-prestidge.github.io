import React, { Component } from "react"
import ReactDOM from "react-dom"

class NoteProject extends Component {
  render() {
    return (
      <div>
        <p> this is where the note project stuff is gonna go!</p>
      </div>
    )
  }
}

export default NoteProject
