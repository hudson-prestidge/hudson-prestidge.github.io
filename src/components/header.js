import React, { Component } from "react"
import ReactDOM from "react-dom"

class Header extends Component {
  render() {
    return (
      <header>
        <h1> Hudson's Web Portfolio </h1>
      </header>
    )
  }
}

export default Header
