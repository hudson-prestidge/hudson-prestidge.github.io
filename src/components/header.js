import React, { Component } from "react"
import ReactDOM from "react-dom"

class Header extends Component {
  render() {
    return (
      <header>
        <p> Hudson's Web Portfolio </p>
      </header>
    )
  }
}

export default Header
